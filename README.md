# Todos API
Simple API for ToDo app

Based on FastAPI

### Main user's features
1. CRUD for todos
2. List todos

### How to start
After git clone install all project's dependencies from requirements.txt:
#### `pip install -r requirements.txt`

And start the project:
#### `uvicorn main:app`
